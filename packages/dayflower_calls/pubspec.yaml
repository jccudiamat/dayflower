name: dayflower_calls
version: 0.0.1
publish_to: none
environment:
  sdk: '>=3.2.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
flutter:
  plugin:
    platforms:
      android:
        package: com.dayflower.calls
        pluginClass: DayflowerCallsPlugin
