<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The adaptive icon's background is @drawable/ic_launcher_background
         now — the artwork's own peach gradient rather than a flat colour, so
         it matches the legacy icon exactly. A colour of the same name left
         here would resolve for anything still asking for @color/… and be a
         different peach, silently. -->
</resources>